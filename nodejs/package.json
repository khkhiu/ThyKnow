{
  "name": "thyknow-express",
  "version": "1.1.0",
  "description": "ThyKnow Telegram bot for self-awareness and connections with weekly streak system",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/src/server.js",
    "dev": "nodemon --exec ts-node src/server.ts",
    "dev:frontend": "node scripts/dev-build-frontend.js --watch",
    "build": "npm run build:backend && npm run build:frontend && npm run copy-public",
    "build:backend": "tsc -p tsconfig.json",
    "build:frontend": "npm run bundle:main && npm run bundle:pet && npm run bundle:streak && npm run copy-static",
    "bundle:main": "esbuild public/miniapp/src/main.ts --bundle --outfile=public/miniapp/dist/main.js --format=esm --target=es2020 --sourcemap",
    "bundle:pet": "esbuild public/miniapp/src/pet.ts --bundle --outfile=public/miniapp/dist/pet.js --format=esm --target=es2020 --sourcemap",
    "bundle:streak": "esbuild public/miniapp/src/streak.ts --bundle --outfile=public/miniapp/dist/streak.js --format=esm --target=es2020 --sourcemap",
    "bundle:config": "node esbuild.config.js",
    "bundle:watch": "node esbuild.config.js --watch",
    "copy-static": "node scripts/copy-static-files.js",
    "copy-public": "cp -r public dist/ && mkdir -p dist/public/miniapp/dist && cp -r public/miniapp/dist/* dist/public/miniapp/dist/",
    "fix-imports": "node scripts/fix-html-imports.js",
    "clean": "rm -rf dist && rm -rf public/miniapp/dist",
    "clean:all": "npm run clean && rm -rf node_modules && npm install",
    "railway:up": "railway up",
    "railway:status": "railway status",
    "railway:logs": "railway logs",
    "setup-webhook": "ts-node scripts/setup-railway-webhook.ts",
    "railway:webhook": "railway run ts-node scripts/setup-railway-webhook.ts",
    "lint": "eslint --ext .ts src/",
    "lint:fix": "eslint --ext .ts src/ --fix",
    "test": "mocha --require ts-node/register test/**/*.test.ts",
    "test:coverage": "nyc --reporter=lcov --reporter=text npm test",
    "copy-images": "node scripts/copy-images.js",
    "verify-build": "node -e \"console.log('Checking build output...'); const fs = require('fs'); ['public/miniapp/dist/main.js', 'public/miniapp/dist/pet.js'].forEach(file => { try { fs.accessSync(file); console.log('✓', file, 'exists'); } catch { console.log('✗', file, 'missing'); } });\"",
    "migrate:weekly": "ts-node scripts/migrate-to-weekly-streaks.ts",
    "migrate:rollback": "ts-node scripts/migrate-to-weekly-streaks.ts --rollback",
    "verify:weekly": "ts-node scripts/verify-weekly-system.ts",
    "db:info": "npx ts-node scripts/database-utility.ts info",
    "db:init": "npx ts-node scripts/database-utility.ts init", 
    "db:verify": "npx ts-node scripts/database-utility.ts verify",
    "db:reset-versions": "npx ts-node scripts/database-utility.ts reset-schema-versions",
    "db:test": "npx ts-node scripts/railway-db-test.js",
    "railway:db:info": "railway run npm run db:info",
    "railway:db:init": "railway run npm run db:init",
    "railway:db:verify": "railway run npm run db:verify",
    "railway:db:test": "railway run npm run db:test",
    "postinstall": "npm run build",
    "railway:deploy": "npm run build && npm run db:migrate",
    "start:production": "npm run db:init && node dist/src/server.js"
  },
  "keywords": [
    "telegram",
    "bot",
    "journaling",
    "self-awareness",
    "connections",
    "railway",
    "miniapp",
    "typescript",
    "esbuild",
    "weekly-streaks",
    "gamification",
    "habit-tracking"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@railway/cli": "^4.5.3",
    "agentkeepalive": "^4.6.0",
    "axios": "^1.9.0",
    "commander": "^14.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "joi": "^17.13.3",
    "moment-timezone": "^0.6.0",
    "morgan": "^1.10.0",
    "node-cron": "^4.1.0",
    "node-fetch": "^3.3.2",
    "npm-check-updates": "^18.0.1",
    "pg": "^8.16.0",
    "telegraf": "^4.16.3",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/joi": "^17.2.3",
    "@types/mocha": "^10.0.10",
    "@types/morgan": "^1.9.10",
    "@types/node": "^24.0.1",
    "@types/node-cron": "^3.0.11",
    "@types/pg": "^8.15.4",
    "@typescript-eslint/eslint-plugin": "^8.34.0",
    "@typescript-eslint/parser": "^8.34.0",
    "esbuild": "^0.25.5",
    "eslint": "^9.28.0",
    "mocha": "^11.6.0",
    "nodemon": "^3.1.10",
    "nyc": "^17.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "railway": {
    "build": {
      "command": "npm run build"
    },
    "deploy": {
      "startCommand": "npm run start:production"
    }
  }
}
