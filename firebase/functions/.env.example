# Firebase project configuration
FIREBASE_PROJECT_ID=your-project-id

# Region where your functions are deployed (default: us-central1)
REGION=us-central1

# Function name that handles the webhook
WEBHOOK_FUNCTION_NAME=botWebhook

# Optional: Telegram Bot Token (normally retrieved from Firebase config)
# TELEGRAM_BOT_TOKEN=your_bot_token